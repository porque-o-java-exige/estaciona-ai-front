import { Routes } from '@angular/router';
import { SignUp } from './pages/sign-up/sign-up';

export const routes: Routes = [
    {
        path: '',
        component: SignUp,
        title: 'Estaciona Ai - Cadastro'
    }
];
